# Core ML stack (needed from Milestone 3 onward — GPU notebooks install their own)
torch>=2.1.0
transformers>=4.50.0
peft>=0.10.0
bitsandbytes>=0.43.0
accelerate>=0.28.0
datasets>=2.18.0
trl>=0.8.0

# Data generation + evaluation (needed for Milestones 1-3, pure CPU)
scikit-learn>=1.4.0
pandas>=2.0.0
tqdm>=4.66.0

# Training / experiment tracking
wandb>=0.16.0

# Demo + reporting
gradio>=4.0.0
matplotlib>=3.8.0
huggingface_hub>=0.22.0
